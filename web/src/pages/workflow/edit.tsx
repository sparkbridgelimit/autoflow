import "reactflow/dist/style.css";
import { memo } from "react";
import WorkflowInit from "./init";

const WorkflowEdit = () => {
  return (
    <>
      <WorkflowInit />
    </>
  );
};

export default memo(WorkflowEdit);
